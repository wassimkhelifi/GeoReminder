class NamedNotification{
  String title;
  DateTime scheduledDate;
  String address;
  int id;
  String description;
  bool active;

  NamedNotification(this.scheduledDate, this.address, this.id, this.title, this.description, this.active);
}